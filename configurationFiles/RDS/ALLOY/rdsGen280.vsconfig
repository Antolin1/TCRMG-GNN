import epackage	"inputs/rdsSimplified.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package rdsSimplified }
	constraints = { package queries }
	solver = AlloySolver
	scope = {
		#node += 20,
		#<rdsSimplified::Database> += 1..1
	}
	
	config = {
		runtime = 120,
		log-level = normal,
		"path" = "/usr/bin/z3"
	}
	
	runs = 1
	number = 30
	
	debug =			"outputs280/debug"
	log =			"outputs280/log.txt"
	output =		"outputs280/models"
}
